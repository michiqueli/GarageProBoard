# Sesión del 16/09/2026 — Los permisos llegan a la pantalla

Arrancó con una duda: si lo del 15 había quedado anotado. Sí — la bitácora se escribió
al final y entró en el mismo commit. Se verificó igual, punto por punto, contra el
código: el router y sus guardias, el arreglo de `establecer()`, `tsx` fuera de la API,
el permiso declarado en el contrato, el `anyAction` de CASL, los cuatro usuarios de
ejemplo. Los conteos de tests también dan: 36 · 42 · 9 · 49.

De paso quedó claro que **verificar una bitácora contra el disco cuesta diez minutos**, y
que conviene hacerlo cuando pasa más de un día: lo que está escrito envejece al ritmo del
código, no al del documento.

## El pendiente que cierra el Hito 1

El criterio del hito es «lo que ve depende de sus permisos». Desde ayer la API los
aplica; la pantalla todavía no. Un mecánico veía el botón «Nuevo vehículo», lo apretaba,
y recién ahí se enteraba. El menú le ofrecía Caja.

### La regla se lee, no se repite

La declaración ya existía en el contrato. Lo que faltaba era poder leerla desde el front
sin copiarla:

    const puedeCrear = usePuedeUsar(contrato.vehiculos.crear)

Para eso, `packages/contracts` ganó tres funciones que antes estaban desparramadas o
metidas adentro de oRPC:

| | |
| --- | --- |
| `accesoDeRuta(ruta)` | Qué declara la ruta. Revienta si no declara nada. |
| `nombreDeRuta(ruta)` | «POST /vehiculos», para que el error diga de qué habla. |
| `permite(habilidades, acceso)` | Si alcanza. **La usan los dos lados.** |

`permite()` es una sola función a propósito. Mientras la API evaluaba
`habilidades.cannot(...)` y el front hubiera evaluado lo suyo, las dos reglas eran la
misma **por casualidad**. El día que alguna crezca —un permiso que dependa del objeto,
por ejemplo— tienen que crecer juntas o el síntoma es un botón que al apretarlo contesta
«no tenés permiso».

`accesoDeRuta` revienta en vez de devolver `undefined`, y eso cambió `@Operacion`: la
verificación quedó en un solo lugar. Devolver `undefined` obligaba a cada quien a decidir
qué hacer con ese caso, y alguna vez alguien iba a decidir «dejala pasar».

### `requiere` es obligatorio en el `Shell`

La pieza que hace que esto no se olvide:

    <Shell titulo="Vehículos" requiere={accesoDeRuta(contrato.vehiculos.listar)}>

Es el equivalente de `@Operacion` en la API. **Una pantalla nueva no compila hasta decir
qué permiso pide**, y con eso sola queda cubierta: el `Shell` decide si dibuja el
contenido o el aviso de permiso, y de paso no renderiza las acciones del encabezado.

Se evaluó ponerlo en la ruta, como `staticData`, o en un `errorComponent`. Contra la
ruta: el aviso tiene que verse **adentro** del armazón, con el menú y la barra, porque
quien llegó por un favorito viejo tiene que poder seguir trabajando. Contra el
`errorComponent`: un boundary que atrapa «sin permiso» atrapa también los errores de
verdad, y los muestra con el mismo cartel amable.

### Tres estados que se ven distinto

El menú tenía dos y ahora tiene tres, y la diferencia es deliberada:

| Situación | Qué se ve |
| --- | --- |
| Sin permiso | **Nada.** No es que falte: no es para este usuario. |
| Sin pantalla construida | Atenuada, sin enlace. No está — para nadie. |
| Todo bien | Enlace normal. |

Una sección sin permiso **atenuada** sería una invitación a preguntar por algo que no le
corresponde. Una sección sin construir **escondida** haría que el menú cambiara de forma
cada semana, y el menú es el lugar donde uno aprende dónde está cada cosa.

### El botón que no está tampoco se dispara

`<Boton accion="global.nuevo">` hace tres cosas: dibuja la tecla, registra el atajo y lo
anuncia en la barra de estado. Al no renderizarlo, las tres se caen juntas — que es
exactamente lo que se quiere. **La tecla sigue apareciendo en la barra, atenuada**, según
la regla de que la barra es un lugar fijo donde mirar y no una lista que cambia de largo.

Lo mismo con el verbo del módulo: sin permiso para cerrar una orden, F4 no se registra.
Un atajo anunciado que no hace nada es peor que uno que no se muestra.

## El inicio ya no lleva siempre al mismo lado

Era `redirect({ to: '/vehiculos' })` fijo. Con los permisos aplicados, un repuestero
—que no ve vehículos— entraba directo al aviso de «no tenés permiso». Un recibimiento
pésimo para alguien que hizo todo bien.

Ahora el inicio manda a **la primera sección que ese usuario pueda ver**, en el orden del
menú. Consecuencia visible: el gerente ahora cae en Órdenes de trabajo y no en Vehículos,
porque Órdenes va primero en el menú. Es el orden en que pensamos el trabajo del día, así
que se deja así hasta que exista el tablero.

Y si no puede ver ninguna —un usuario recién creado, sin rol— hay una pantalla que lo
dice: «Tu usuario está bien, le falta el rol». Antes habría sido una página en blanco, y
una página en blanco se lee como «el sistema está roto» cuando en realidad está cerrado,
que es lo correcto.

## Un detalle de tipos que valía la pena arreglar

`habilidades` viajaba en el contrato como `z.array(z.unknown())`: el front recibía las
reglas de permisos como un array de nada y tenía que castear para usarlas. Ahora hay un
esquema `reglaPermiso` de verdad, y `DatosSesion.habilidades` es `ReglaPermiso[]`.

Eso destapó algo real: `ReglaPermiso` declaraba `conditions?: Record<string, unknown>`
sin `| undefined`, y con `exactOptionalPropertyTypes` eso **promete** que si la clave
está, tiene valor. Una regla que llega de la base —o de zod— puede traerla en `undefined`.
Los cuatro opcionales lo dicen explícito ahora.

## Tests

**52 en `apps/web`** (eran 42). Los 10 nuevos montan la aplicación entera con los roles
que siembra la base, no con permisos inventados: si mañana el mecánico deja de ver
vehículos, el test se entera.

Cubren el menú con permiso total, el menú del mecánico, la diferencia entre atenuada y
escondida, el botón de alta que aparece y el que no, la barra de estado atenuada, la
pantalla sin permiso con el menú todavía a mano, que **no se le pida el listado a la API**
cuando ya se sabe que va a dar 403, el inicio de un repuestero, el usuario sin rol y el
F4 que no dispara.

| | |
| --- | --- |
| Tests | **146** — 36 core · 52 web · 9 db · 49 api |

Corridos hoy: core (36), web (52) y los 9 de acceso de la API, que no necesitan
contenedor. **Los otros 40 de la API y los 9 de `db` no se corrieron**: Docker Desktop
está instalado pero no estaba levantado, y usan Testcontainers. `typecheck` y `lint`,
limpios en todo el workspace.

## Lo que se decidió sin construir: módulos por flags

Pedido de hoy, y de los que cambian el orden del plan: **todos los módulos se activan y
desactivan por tenant, desde el día cero**, más un **panel de administración aparte** —
posiblemente en otro dominio— para manejar tenants, módulos y planes.

No es una función para más adelante: es cómo se vende el producto. Y aplica la misma
regla que el aislamiento y los permisos: meterlo después obliga a revisar cada pantalla
y cada ruta ya escritas, y nunca va a haber menos código que hoy.

Quedó como **Hito 1.5**, antes de las pantallas del núcleo, con el mapa propuesto y las
cinco preguntas abiertas en [módulos del sistema](../tecnicos/modulos-del-sistema.md).
La más importante para no equivocarse: **«no contratado» y «sin permiso» no son lo
mismo**, ni para el filtro ni para el mensaje. Al que no tiene el módulo no se le habla
de permisos, porque no hay permiso que le sirva.

---

## Los módulos, cerrados en la misma sesión

Las cinco preguntas abiertas de la mañana se contestaron a la tarde. Están todas en
[módulos del sistema](../tecnicos/modulos-del-sistema.md); acá va lo que cambió de
cabeza.

### La orden de compra obligó a mover el flag de lugar

«Se genera en contable y se ejecuta en repuestos». Con eso se cae la idea de que cada
entidad pertenece a un módulo: elegir un dueño para `orden_compra` habría sido falso en
cualquiera de las dos direcciones.

> **El flag pertenece a la pantalla y a la operación, no al dato.**

La tabla es una sola, del núcleo. La pantalla de generación declara `contable`; la de
recepción de mercadería, `repuestos`. Una concesionaria con contable y sin repuestos
genera órdenes de compra y no las recibe por sistema: es una combinación vendible, no un
agujero.

### El núcleo también se apaga

Podría haber sido una excepción en el código —«el núcleo no lleva flag»— y no lo es. El
plan mínimo lo deja prendido y listo. Una excepción escrita en el código es una que
mañana alguien va a querer sacar y no va a poder.

Lo que aparece a cambio es la necesidad de **declarar dependencias entre módulos**:
servicios no abre una OT sin vehículos. Apagar el piso de un módulo prendido se rechaza
en el back-office, con el motivo escrito, y no se descubre cuando un usuario abre la
pantalla.

### La configuración vive adentro de cada módulo

Propuesta del usuario, contra lo que se hace siempre —un módulo Configuración con todo
adentro—. Coincido, y el argumento que la cierra no es el que parece: no es que sea más
accesible, es que **se apaga junto con el módulo**. Con una Configuración global
quedarían seis secciones muertas que no se pueden apagar de a una.

Con la aclaración de que hay **tres clases** y sólo una es del módulo: la del usuario
(tema, densidad, teclas) va en una pantalla sola; la de la concesionaria (razones
sociales, sucursales, usuarios) en Administración; la del módulo, adentro. Los
certificados de AFIP cuelgan de la `empresa` —dos razones sociales, dos certificados—
aunque la pantalla viva en contable.

### Entrar a un módulo no es poder configurarlo

La pregunta del usuario era si cualquiera que accede al módulo debería poder tocar su
configuración, y la respuesta es no: **la cuenta de Mercado Pago y los certificados de
AFIP son credenciales, no preferencias.** Quien las toca puede facturar con el CUIT de
la empresa o mandar la cobranza a otra cuenta.

La solución no es un rol «Contador encargado» escrito en el código, sino **una acción más
en el catálogo**: `configurar`. `configurar Comprobante` habilita el engranaje de
contable, `configurar Orden` el de servicios, y el Gerente lo tiene gratis por
`administrar all`. Si una concesionaria quiere separar contador de contador encargado,
lo arma ella clonando el rol: el organigrama de cada concesionaria no lo decidimos
nosotros.

Y **la configuración del módulo no es el flag del módulo**: la primera la toca el
cliente, el segundo sólo nosotros. Es la decisión 14 de GarageTick (`setting` vs
`feature`), y el motivo es el mismo — si viven juntas, el cliente se habilita solo lo
que no pagó.

## GarageTick entra como base del taller

El usuario copió su aplicación anterior adentro del repo, en `GarageTick/`, para que sea
la base de todo lo del taller. Es la pieza de GaragePro que **no** estamos razonando sin
piloto: está relevada con una concesionaria de verdad.

Lo que trae, en [control de tiempos de taller](../tecnicos/taller-control-de-tiempos.md):
el requisito de latencia con números (**2 escaneos, 3 segundos, sin login**), la
separación entre sesión, operación y estado de la OT —que es donde estos proyectos se
hunden—, los motivos de espera, el QR firmado, el auto-cierre de fin de turno y la
máquina de estados del kiosco.

Lo que se cae, y es la mejor noticia: **toda la integración con Oversoft**. Era leer
órdenes por SQL Server sobre una vista que tenía que habilitar el competidor, sincronizada
a Postgres para que el escaneo nunca dependiera de una consulta ajena. Ahora la OT nace
acá y **el QR se imprime en nuestra propia orden**: desaparecen el adaptador, el sync y
la fricción de etiquetar una orden que imprimió otro sistema.

Queda pendiente decidir dónde vive esa carpeta: tiene su propio `.git` y su propio stack,
así que adentro del monorepo confunde a los dos repositorios.

## Un buscador del sistema en la barra lateral

Pedido del usuario: un campo en la barra lateral donde escribir «mercado pago» y llegar a
lo de Mercado Pago, o «certificados» y llegar a los de AFIP. Anotado en el
[sistema de diseño](../tecnicos/sistema-de-diseno.md#buscador-del-sistema), no
implementado todavía.

Lo que hubo que decidir para poder escribirlo:

- **No es el buscador del encabezado.** Ese busca **datos** —patente, chasis, cliente— y
  vive en `F3`. Este busca **el sistema**: pantallas, acciones y configuraciones. Dos
  cajas con dos preguntas distintas, y cada una lo dice en su propio texto de ayuda.
- **Es la misma cosa que la paleta de `F10`.** El campo es la puerta visible y la tecla
  el atajo; un segundo índice sería un segundo lugar donde olvidarse de registrar algo.
- **Lo que se busca son palabras clave, no títulos.** Nadie escribe «Certificados»
  cuando lo que tiene en la cabeza es «factura electrónica». Cada entrada declara sus
  sinónimos, y ése es el trabajo de verdad del índice.
- **Los mismos tres estados del menú**: sin permiso o con el módulo apagado no aparece;
  sin construir aparece atenuada y dice dónde va a estar. Contestar «¿dónde va a estar
  Mercado Pago?» antes de que la pantalla exista es mejor que el silencio.
- **El resultado muestra la miga** —«Contable → Configuración»— porque enseña el mapa:
  quien busca dos veces, la tercera va por el menú.

Quedó como regla 9 de la skill `ui-garagepro`: **una pantalla nueva se registra en el
índice igual que se registra en el menú.**

De paso salió una deuda que no estaba anotada: **en teléfono no hay menú**. La barra
lateral está oculta por debajo de `md` y no hay nada que la reemplace. Como el buscador
vive ahí, las dos cosas se resuelven juntas.

## GarageTick, podado

Se dejó en `GarageTick/` solamente lo que se va a copiar, con un README que explica qué
es cada cosa. Aparecieron dos sorpresas al abrirlo:

1. **Faltaban `packages/shared` y `packages/db`**: estaban borrados en la copia, y ahí
   viven el codec del QR, la firma HMAC y el esquema de 15 tablas — o sea, exactamente lo
   que se quería conservar. Se recuperaron del historial.
2. **Tres commits nunca se pushearon** a GitHub, entre ellos el que agrega la aplicación
   web entera. El respaldo remoto no alcanzaba. Antes de podar se guardó la historia
   completa en `garagetick-historia.bundle`, fuera del repositorio.

Lo descartado —`node_modules`, la configuración del monorepo viejo y los documentos que
ya no aplican— se movió a `garagetick-descartado/`, también afuera. Nada se borró.

### Y una tercera, al aparecer la carpeta original

El original está en `C:/Programacion/Trabajo/Propios/GarageTick`, y la copia **se había
cortado**. Dos cosas más:

- **La copia se llevó el `.git`**, no lo duplicó: el original había quedado sin
  repositorio. Se lo devolvimos, y volvió a ser un repo con su historia completa y sus
  tres commits sin pushear.
- **`migrations_es_respaldo/` nunca se copió, y estaba `gitignore`ada.** No estaba en
  ningún commit ni en GitHub: existía en una sola carpeta de un solo disco.

Y esa carpeta ignorada resultó ser **lo más útil de todo**: es el mismo esquema **con los
identificadores en castellano**, de antes de que ese proyecto se renombrara a inglés.
`orden`, `sesion_trabajo`, `orden_espera`, `evento_escaneo`, `ajuste_sesion`,
`credencial`, `dispositivo`. Es el idioma en que nombramos acá, así que para copiar sirve
esa versión y no la que estaba a la vista.

Moraleja incómoda: **el archivo más valioso del día estaba en `.gitignore`.**

## La carpeta del proyecto se renombró, y pnpm no se enteró

Al renombrar la carpeta a `GaragePro` dejaron de andar el typecheck y los tests. En
Windows pnpm enlaza los paquetes del workspace con *junctions*, que guardan la **ruta
absoluta**: seguían apuntando a la carpeta vieja. Se arregla con `pnpm install`, que los
regenera; el lockfile no cambia.

De paso, Biome empezó a revisar `GarageTick/` y a fallar por código que no es nuestro.
Quedó excluido en `biome.json`: es material para copiar, no para mantener. Ya no tiene
`.git` —volvió al original—, así que no confunde a ningún repositorio.

Con Docker levantado se corrieron los que faltaban: **146 en verde** — 36 core · 52 web
· 9 db · 49 api.

## Hito 1.5, primera mitad: los módulos se aplican

De las once tareas del hito quedaron hechas cinco: la tabla, las dependencias, la
declaración en el contrato, la API y la pantalla. Falta todo lo que es back-office y
configuración. El detalle está en
[cómo está construido](../tecnicos/modulos-del-sistema.md#cómo-está-construido).

### Primero, una palabra con dos significados

En `core` ya existía `Modulo`, y quería decir otra cosa: la pantalla que define qué hace
`F4` (`ordenes`, `vehiculos`). Con los módulos contratados iban a convivir dos `Modulo`,
y tarde o temprano alguien filtraría las teclas por lo que compró la concesionaria. El
de las teclas pasó a llamarse **`Pantalla`**. Son nombres de TypeScript: lo guardado en
`usuario_atajo` no cambia.

### El módulo va pegado al permiso

    conPermiso('nucleo', 'crear', 'Vehiculo')

No es un `.meta({ modulo })` aparte que se pueda olvidar: **sin módulo no compila**. Al
cambiar la firma, el front dejó de compilar en once lugares —cada sección del menú, cada
`usePuede`—, y ésa fue la lista de lo que había que revisar.

`permite()` devolvía un booleano y ahora hay `evaluarAcceso()`, que devuelve **por qué**:
`permitido`, `modulo-apagado` o `sin-permiso`. Hacía falta porque los dos «no» se
muestran distinto. El orden está escrito en la función, y tiene un test que lo cuida: a
un repuestero sin permiso para ver vehículos, en una concesionaria con el núcleo
apagado, se le contesta lo del módulo. «Pedí permiso» lo mandaría a pedir algo que nadie
le puede dar.

### La tabla que la aplicación no puede escribir

`tenant_modulo` lleva RLS como todas, pero RLS decide **qué filas** ve cada
concesionaria, no si puede escribirlas. Así que además se le sacó al rol de la API el
permiso de escritura. Un bug en una pantalla de administración no puede terminar en un
cliente que se habilitó solo lo que no pagó. Hay tests que lo intentan por las tres
vías: dar de alta, prender uno suspendido y borrar.

**Sin fila, el módulo está apagado**, como los permisos: cerrado por omisión. Eso tenía
una consecuencia incómoda, y es que migrar dejaba sin nada a las concesionarias que ya
existían. La migración las completa con todos los módulos, porque antes tenían todo.

La llave y el período son dos columnas separadas: `activo` suspende sin perder las
fechas del contrato, y `vigente_hasta` apaga sola una prueba de treinta días.

### Lo que se ve

Norte quedó sembrada **sin contable**: entrar como su gerente muestra que Caja desaparece
del menú aunque el gerente pueda todo. Litoral tiene todos.

Por un enlace viejo a una sección apagada aparece «Esta sección no está habilitada en la
concesionaria», sin hablar de permisos y sin ofrecer contratarla. Y el inicio la salta:
el gerente de una concesionaria sin servicios cae en Vehículos y no en Órdenes.

### Un detalle de empaquetado

drizzle-kit carga el esquema en CommonJS, y `@garagepro/core` sólo exportaba la condición
`import`. El esquema ahora lee la lista de módulos del catálogo —para que la restricción
`check` no se desincronice—, así que `core` sumó la condición `default`.

| | |
| --- | --- |
| Tests | **167** — 42 core · 56 web · 16 db · 53 api |

Todos corridos, con Docker. `typecheck` y `lint`, limpios.

## Un rol para dar de alta usuarios

Pregunta del usuario: ¿hace falta un rol de sistemas? Un gerente que no sabe o no quiere
cargar al mecánico nuevo lo va a delegar, en sistemas o en RRHH.

Sí, y va como **rol predefinido** —«Administrador de usuarios»—, no como alguien fijo en
el código. Salieron dos cosas que no eran obvias, anotadas en
[usuarios y roles](../tecnicos/usuarios-y-roles.md):

- **Usuario no es empleado.** El acceso es del núcleo y el legajo de RRHH. Hay empleados
  que nunca entran al sistema, y quien administra usuarios no ve sueldos.
- **Quien reparte roles puede darse cualquiera**, y entonces es gerente con un paso de
  más. Se cierra con dos reglas en la API: nadie da un permiso que no tiene, y nadie
  toca sus propios roles.

Queda para el Hito 2, con la pantalla de usuarios.

## `configurar`, en el catálogo

Entró la acción, entre `facturar` y `administrar`. Ningún rol predefinido la trae —ni el
administrativo, que emite y anula comprobantes— y un test recorre los roles para que
siga así: sumársela a uno tiene que ser una decisión, no un descuido al copiar una
lista de acciones. El gerente la tiene por `administrar all`.

Tests de `core`: 44.

## El back-office

Decidido con el usuario: una aplicación aparte en `apps/backoffice`, con operadores
propios y **sin segundo factor por ahora** —queda anotado en el roadmap—. Se hizo en tres
commits: la base, la API y el panel.

### Un rol de Postgres que no ve lo que no necesita

La salida fácil era un rol con BYPASSRLS. Se descartó: el back-office no tiene por qué
poder leer una orden de trabajo, y un rol que se saltea el aislamiento lo puede todo.

`garagepro_backoffice` no se saltea nada. Ve **todas** las concesionarias sólo en `tenant`
y `tenant_modulo`, con una política propia. Para dar de alta la empresa y el gerente usa
`conTenant()` como la API, y las políticas de siempre lo contienen. Un test intenta que
lea vehículos y que inserte una empresa a nombre de otra concesionaria, y no puede.

Hubo que tocar una política existente, y el motivo no es obvio: en `tenant` y
`tenant_modulo` la de aislamiento pasó a aplicarse **sólo a la API**. Si se aplicara a
todos, el back-office evaluaría `app_tenant_id()` sin tenant en la sesión, y esa función
revienta —a propósito— aunque otra política lo dejara pasar.

Los permisos del rol se dan **desde cero en cada migración**: primero se le saca todo y
después se le da la lista. Sacar una línea de la lista le saca el permiso, en vez de
dejarlo puesto de la corrida anterior.

### La auditoría que el auditado no puede editar

El back-office inserta y lee `auditoria_backoffice`, y nunca la modifica ni la borra. Los
operadores no se crean desde el panel sino con `pnpm db:operador`: quien se lleve una
sesión no puede darse un acceso que sobreviva a que se la corten.

Todo cambio exige **motivo**. Dentro de un año, «¿por qué le apagamos contable?» tiene
que tener respuesta sin preguntarle a nadie.

### El alta, todo o nada

Concesionaria, módulos, razón social, sucursal, los roles predefinidos y el gerente, en
una transacción. Un test da de alta con un correo que ya existe y verifica que no quede
ni la concesionaria. La contraseña del gerente se genera en el servidor y se muestra una
sola vez.

Cambiar un módulo se rechaza si **agrega** una dependencia rota, no si ya había una: una
concesionaria mal cargada se tiene que poder arreglar de a un paso.

### Los tokens de diseño, a un paquete

El panel necesitaba los mismos tokens que la aplicación, y copiarlos rompía la primera
regla del sistema de diseño. Se mudaron a `packages/ui-tokens`, y la skill y el documento
apuntan ahí.

El panel usa **otra marca**: azul en lugar de ámbar. Con la pestaña de un cliente y la del
back-office abiertas, tiene que ser evidente de un vistazo en cuál se está por apagar
algo.

`pnpm dev` no levanta el back-office: es `pnpm dev:backoffice`. Sin sus variables de
entorno no arranca, y no tiene por qué romperle el día a quien sólo trabaja en la
aplicación.

| | |
| --- | --- |
| Tests | **212** — 44 core · 56 web · 28 db · 53 api · 17 back-office api · 8 back-office web |

Con eso se cumple el criterio del Hito 1.5 —se le apaga un módulo a una concesionaria y
desaparece de su sistema, sin tocar código— con una salvedad: el panel no se levantó
contra la base de desarrollo en esta sesión; está probado con tests, no a mano.

## GaragePro pasa a llamarse GarageProBoard

«GaragePro» ya estaba usado. El usuario compró `garageproboard.com` y
`garageproboard.com.ar`, y la marca corta es **GPB**.

El criterio del cambio: **lo que se ve dice GarageProBoard y lo técnico dice `gpb`**. Los
paquetes son `@gpb/*`, los roles de Postgres `gpb_app` y `gpb_backoffice`, la base y los
contenedores de desarrollo `gpb`, y la skill de diseño `ui-gpb`. Las cookies también
cambiaron, y la de refresco se llamaba `gt_refresco`: un resto de GarageTick que nadie
había notado. El repositorio en GitHub se renombró a `GarageProBoard`; la carpeta local
queda como está.

Se hizo ahora y no después por los roles de Postgres: renombrarlos con concesionarias
adentro sería una migración delicada; hoy es un `docker compose down`.

Las bitácoras anteriores no se tocaron: registran cómo se llamaba el producto cuando se
escribieron.

Para seguir trabajando en una máquina que ya tenía el entorno: bajar el Postgres viejo con
`docker compose -p garagepro -f infra/docker-compose.yml down`, copiar los valores nuevos
de `.env.example`, y volver a `pnpm infra:up`, `pnpm db:migrate`, `pnpm db:app-role` y
`pnpm db:sembrar`.

Tests: **212**, todos en verde con el nombre nuevo.

## Probado a mano: apagar el núcleo arrastra, y un reloj que iba adelantado

El usuario probó todo contra la base de desarrollo, y salieron dos cosas.

**Apagar el núcleo se rechazaba** mientras hubiera otro módulo prendido, que era lo
decidido. Usándolo, obligaba a apagar seis módulos de a uno. Ahora apagar un módulo apaga
los que dependen de él, **con la lista a la vista antes de confirmar** y escrita en el
botón: «Apagar Núcleo y 6 módulos más». La API sólo lo hace si se lo pide
(`apagarDependientes`), y cada arrastrado queda en el historial con el motivo y de qué cayó.
`dependientesDe()` en `core` sigue la cadena, por si algún día hay dependencias en
segundo grado.

**Un módulo recién contratado podía figurar «no vigente».** Salió por un test que falló
una vez de tres. La fecha de inicio la ponía el reloj de Node, pero «vigente» se calcula
con el `now()` de Postgres, que es la hora en que *empezó* la transacción: con Node unos
milisegundos adelante, la respuesta decía que el módulo que se acababa de prender estaba
apagado. Ahora la fecha la pone la base. Moraleja: un test que falla una vez no es mala
suerte hasta que se demuestra que lo es.

Tests: **215** — 46 core · 56 web · 28 db · 53 api · 18 back-office api · 9 back-office web.

## Hito 2: usuarios

Primera pantalla del núcleo. Hoy, fuera de la semilla, la única forma de crear un usuario
era el alta de la concesionaria desde el back-office, y creaba sólo al gerente.

Hay alta, modificación, baja y contraseña nueva, con la API y la pantalla. Lo que importa
no es el ABM sino las reglas, y al construirlo salieron dos cosas que no estaban en lo
anotado a la mañana.

### «Nadie da lo que no tiene» deja al administrador de usuarios casi sin nada que dar

`permisosQueNoTiene()` compara cada regla del rol con las de quien lo asigna. Una
prohibición no alcanza, y una regla con condiciones tampoco: el mecánico edita *sus*
órdenes, no puede dar «editar órdenes».

La consecuencia: «Administrador de usuarios» solo no puede asignar ni un mecánico, porque
con esa cuenta vería las órdenes que él no ve. Es correcto, y se resuelve combinándolo
con los roles que esa persona va a repartir. La pantalla lo dice en cada rol —«te falta
ver órdenes de trabajo»— antes de que alguien intente.

### El hueco que no estaba anotado

La regla era «nadie toca sus propios roles». Faltaba el otro lado: generarle una contraseña
nueva al gerente es quedarse con su cuenta. Ahora **nadie modifica a un usuario con algún
permiso que él no tiene**, y en la lista aparece como «Tiene más permisos», sin botón.

La baja y la contraseña nueva cierran las sesiones del usuario: la guardia ya lo dejaba
afuera, pero así tampoco queda una renovación viva esperando que lo rehabiliten.

La pantalla usa Ins para el alta, F2 para guardar y Esc para cancelar, desde los botones
y no escuchando teclas por su cuenta. `Campo` pasó a ser un componente de la aplicación,
con la ayuda enganchada por `aria-describedby`.

Queda para después: crear y editar roles, y registrar Usuarios en el buscador del sistema
cuando exista.

| | |
| --- | --- |
| Tests | **239** — 53 core · 62 web · 28 db · 68 api · 18 back-office api · 9 back-office web |

## El administrador de sistema, y lo que lo controla

El usuario lo pidió al probar: quien administra el sistema tiene que poder hacer todo, y
que quede registrado. Se revirtió la regla estricta para ese rol —queda para quien sólo
da de alta usuarios— y se construyó lo que la reemplaza.

### La idea de la IP, y por qué no alcanzaba

La propuesta era registrar la IP de cada ingreso para saber desde qué PC se entró. El
problema: todas las computadoras de una concesionaria salen por el mismo router, y el
servidor ve la misma IP para el gerente y para el de sistemas. Las internas no llegan.

Lo que sí distingue una PC de otra es **el navegador**: la primera vez recibe un
identificador en una cookie `httpOnly` de dos años, y cada sesión queda asociada a él.
Una cookie inventada no se adopta, y una app sin cookies no registra computadora.

### Tres piezas

- **Computadoras**, con nombre que les pone la concesionaria. El cambio de nombre se
  audita: es la forma obvia de borrar huellas.
- **Avisos** al usuario afectado cuando otro le cambia contraseña, roles o sucursales:
  «Tu contraseña la cambió Juan Pérez el 16/9/26, 10:32». Llegan con la sesión, se ven
  arriba de cualquier pantalla y se marcan como leídos.
- **Auditoría**, una pantalla con ingresos, cambios y computadoras. Un ingreso cuenta una
  vez por inicio de sesión —las renovaciones de cada quince minutos no— y va marcado
  cuando es la primera vez que ese usuario entra desde esa computadora.

El test que justifica todo es literal: el gerente entra dos veces desde su PC, alguien
entra con su cuenta desde la PC de sistemas, y la tercera fila dice «Computadora nueva».

| | |
| --- | --- |
| Tests | **263** — 53 core · 67 web · 28 db · 84 api · 18 back-office api · 9 back-office web |

## Empresas, sucursales y puntos de venta

Una pantalla con el árbol entero —razón social, sus sucursales, los puntos de venta de
cada una— porque así lo piensa quien lo carga.

El usuario marcó un caso mientras se construía: **una SAS por sucursal**. El modelo ya lo
soportaba —cada sucursal pertenece a una razón social, y una concesionaria puede tener
todas las que quiera— y la API lo prueba: dos SAS del mismo grupo, cada una con su punto
de venta 1. La empresa del punto de venta sale de la sucursal y no del pedido, así que no
hay forma de pedir uno de la SAS B para una sucursal de la SAS A.

**El CUIT se valida con el dígito verificador**, en `core`, y lo usan el contrato y la
pantalla. Al hacerlo apareció que las semillas y un test usaban CUIT inventados con el
dígito mal: los corregimos. El CUIT de AFIP, 33-69345023-9, es uno de los casos del test.

Nada se borra. Desactivar una sucursal se rechaza si hay usuarios que sólo entran a esa
—quedarían sin poder entrar— y el mensaje dice quiénes. Quien da de alta una sucursal
queda con acceso, para poder entrar a cargarla. Marcar un punto de venta como
predeterminado desmarca al anterior, y desactivarlo le saca la marca.

### Lo que viene: padrón de AFIP

El usuario dejó los certificados de homologación en `secretos/afip/`, que está en
`.gitignore`, y va a copiar su aplicación ecoparrilla a `referencias/`, también ignorada:
ahí usa `@arcasdk/core` para facturar, para el padrón A13 y para la constancia de
inscripción. Es la base para autocompletar razón social, condición frente al IVA y
domicilio a partir de un CUIT.

| | |
| --- | --- |
| Tests | **291** — 57 core · 72 web · 28 db · 97 api · 18 back-office api · 9 back-office web |

## El padrón de AFIP

El usuario dejó los certificados en `secretos/afip/` y su aplicación ecoparrilla en
`para_sacar_codigo/` —junto con GarageTick, que se mudó ahí—. Una advertencia sobre ese
movimiento: ocurrió mientras se commiteaba la pantalla de empresas, y **el commit
`229aa44` se llevó el borrado de `GarageTick/`** aunque su mensaje no lo dice. No se perdió
nada: está en `para_sacar_codigo/` y en la historia.

### Lo que se trajo de ecoparrilla, y lo que no hizo falta

La lógica de consulta y de condición frente al IVA vino tal cual: primero la constancia de
inscripción, que es la única que informa impuestos y monotributo; si falla o no encuentra,
el A13. Monotributo → 6; IVA (impuesto 30) activo → 1; régimen general sin IVA → 4; una
sociedad sin datos se sugiere Responsable Inscripto, marcado como deducido.

Lo que no hizo falta: ecoparrilla usa `@arcasdk/core` 0.3.6 y esquivaba un error del SDK
yendo al cliente SOAP a mano. En la 2.0.2, que es la de acá, ese error está corregido.

### Lo que apareció al probar contra AFIP

Antes de escribir el adaptador se consultó AFIP de verdad con tres CUIT: el de la propia
AFIP, uno de persona física y uno inexistente. Salieron dos cosas que los tipos del SDK no
dicen:

- **Un CUIL no tiene constancia, pero el A13 lo encuentra.** `20-99999998-1` no está en la
  constancia y el A13 lo devuelve con `tipoClave: CUIL`. La pantalla avisa: un CUIL no
  factura.
- **Un CUIT inexistente, el A13 no lo devuelve vacío: tira un SOAP Fault** («La Clave
  consultada es inexistente»), que el SDK no traduce. Se reconoce aparte; si no, el
  sistema decía «AFIP caída» en vez de «ese CUIT no existe».

**El certificado es de producción** —lo emitió «AFIP Computadores», no «Computadores
Test»—. Para el padrón da igual. Para facturar, el usuario autorizó pruebas con montos de
$1; se van a hacer como pasos explícitos y avisados, nunca desde los tests automáticos.

### Cómo quedó

- `packages/afip`: `padron.ts` interpreta, puro y con tests sobre respuestas reales
  (la de persona física, anonimizada); `padron-arca.ts` es el único archivo que conoce el
  SDK.
- La credencial es **de GarageProBoard**, no de cada concesionaria: autocompletar un CUIT
  no tiene por qué esperar a que el cliente cargue sus certificados.
- La API recuerda cada respuesta doce horas —también el «no existe»— y no recuerda los
  errores. Sin configurar, arranca igual y contesta «no disponible».
- En el alta de una razón social, «Completar con los datos de AFIP» llena razón social,
  condición, domicilio y provincia. No guarda: quien carga revisa, y la pantalla avisa si la
  condición es deducida, si es un CUIL o si está inactivo.

| | |
| --- | --- |
| Tests | **300** — 57 core · 12 afip · 73 web · 28 db · 103 api · 18 back-office api · 9 back-office web |

## Certificados de facturación: camino A, con asistente

Se aclaró de quién es cada credencial: **el padrón es de GarageProBoard**, una sola en el
servidor; **facturar es de cada razón social**, con su CUIT.

Para facturar se evaluaron dos caminos —el certificado de cada cliente, o que el cliente
delegue el servicio en el CUIT de GarageProBoard— y el usuario eligió **el de cada
cliente**, con un asistente que le saque lo difícil: el sistema genera la clave y el pedido,
y guía los pasos en ARCA. El orden de los pasos lo dio el usuario, que los hizo en
ecoparrilla: crear el pedido, crear el computador en ARCA, subir el certificado, activar la
facturación **asociada al computador y no al CUIT**, crear el punto de venta con RECE o
Monotributo según la condición, y probar. Detalle en
[certificados de AFIP](../tecnicos/afip-certificados.md).

**¿Hace falta MinIO?** Para los certificados, no: son 3 KB y van cifrados en Postgres, detrás
de RLS y en la misma transacción que su verificación. Para lo que viene —fotos de la
recepción, documentación escaneada, adjuntos de órdenes, logos— sí hace falta un almacén de
archivos. Se programa contra la API de S3 y se elige el producto en el Hito 4.

**La fecha de inicio de actividades** no la devuelve ninguno de los dos padrones habilitados
—sólo periodos de impuestos y actividades—, y los A4, A5 y A10 contestan «computador no
autorizado». Queda como campo manual: va impresa en cada factura, y un mes deducido no
alcanza.

## Clientes

Se retomó con un «¿seguimos?» y el primer punto de la lista. El esquema ya estaba desde el
modelo del núcleo —`entidad_comercial` con la identidad fiscal, `cliente` como rol—, y el
permiso y la entrada del menú también. Faltaban el contrato, la API y la pantalla.

### Un cliente es un rol, y eso se nota en el alta

Si el CUIT ya está cargado como proveedor, el alta **no crea otra ficha**: le suma el rol
de cliente a la misma identidad, con los datos que se acaban de revisar. La auditoría lo
cuenta así —«Lo dio de alta sobre una identidad fiscal que ya existía y domicilio: …»—,
porque el alta pisó datos que otro había cargado y eso tiene que verse. En la pantalla,
modificar a quien es las dos cosas avisa que el domicilio cambia para los dos.

Si el documento ya es cliente, el error dice **a nombre de quién** y ofrece buscarlo, con
los desactivados incluidos: el caso típico es el cliente dado de baja que vuelve.

Las dos altas simultáneas del mismo documento no dependen de una consulta previa: la
identidad y el rol se insertan con `on conflict do nothing`, y la segunda se entera al no
recibir fila.

### El documento, según su tipo

CUIT, CUIL o DNI. El 99, «sin identificar», quedó afuera a propósito: es el consumidor
final anónimo de un comprobante, no alguien que se carga. CUIT y CUIL se validan con el
dígito verificador, el DNI por largo. Persona física o jurídica se deduce del prefijo del
CUIT, y la condición frente al IVA arranca en Consumidor Final, que es lo que entra por el
mostrador; el padrón la corrige cuando hay CUIT. Si alguien eligió CUIT y escribió un CUIL,
lo corrige AFIP al completar.

`F4` en Clientes es «Completar con AFIP» —la acción `clientes.consultarPadron` ya estaba en
el catálogo de atajos—. Con DNI no aparece: no hay padrón que consultar.

### El buscador

Nombre, CUIT con o sin guiones, DNI con o sin puntos. Un DNI encuentra también el CUIL que
lo contiene. Por omisión sólo activos: al desactivado no se le ofrece facturar.

### De paso

`Selector` y el resultado del padrón salieron de la pantalla de empresas a `componentes/`,
porque ahora los usan dos pantallas.

### La auditoría dice nombres, no códigos

Al probar el alta sobre un proveedor apareció «provincia: «vacío» → «12»», y lo mismo pasaba
en empresas. Se arregló en la misma sesión: al contar los cambios se leen las condiciones
frente al IVA y las provincias del catálogo, y los valores fijos —la condición en Ingresos
Brutos, el uso de un punto de venta— se dicen como en la pantalla. Empresas y clientes
comparten ahora la misma función para listar qué campo cambió. Como la auditoría se cuenta
al leer, los registros viejos también se leen bien, sin reescribir nada.

| | |
| --- | --- |
| Tests | **323** — 57 core · 12 afip · 82 web · 28 db · 117 api · 18 back-office api · 9 back-office web |

## Vehículos completo

Se siguió sin cortar. Hasta acá Vehículos tenía listado y un botón «Nuevo vehículo» que no
hacía nada.

### El titular se transfiere, no se edita

La ficha no tiene un campo «titular» para pisar: tiene **Transferir** (`F4`). Cierra la
titularidad vigente el día indicado y abre la nueva desde ese mismo día; la anterior queda
con su rango. La fecha no puede ser anterior a la del titular actual —el error dice desde
cuándo lo tiene— ni posterior a hoy, y hoy es **hoy en la Argentina**: el servidor puede
correr en UTC, y una transferencia cargada a las nueve de la noche no puede rebotar por
futura. Dos transferencias del mismo auto a la vez se esperan con `select … for update`.

A un cliente desactivado no se le transfiere. El alta acepta titular opcional: un 0km en
stock no es de nadie.

### Marca y modelo, escritos

No hay un ABM de marcas que llenar antes del primer auto: se escriben, se sugieren las que
ya están, y si no existen se crean. Se reconocen sin importar mayúsculas —«TOYOTA» es
Toyota—. **Van los dos o ninguno**: el vehículo guarda el modelo y el modelo sabe su marca,
así que una marca sola se habría perdido en silencio.

### La historia

La ficha cuenta lo que se le hizo con la misma auditoría de siempre: «Patente: «vacío» →
«AF456CD»», «Pasó de Transportes del Sur SRL a Gómez, Ana, desde el 15/06/2025». Apareció un
detalle al probar: el alta y su primer titular van en la misma transacción, comparten la
hora exacta, y como los ids son al azar el orden salía a la suerte. Con la misma hora, el
alta va primero.

Las órdenes de trabajo todavía no existen; la historia lo dice abajo, en vez de aparentar
estar completa.

### Lo que se ve

- El listado muestra la patente como en la chapa —«AE 123 BC»—, el vehículo y el titular, y
  la patente lleva a la ficha. El buscador encuentra también por titular.
- El selector de cliente se usa entero con teclado: se escribe, `↓` baja, `Enter` elige.
  Enter en el campo elige el primero en vez de mandar el formulario a medio cargar.
- `F9` en la ficha lleva a la historia.

| | |
| --- | --- |
| Tests | **348** — 57 core · 12 afip · 92 web · 28 db · 132 api · 18 back-office api · 9 back-office web |

## La búsqueda del encabezado

El campo del encabezado estaba dibujado y no hacía nada. Ahora `F3` lo enfoca y busca
patente, chasis, titular, cliente o CUIT desde cualquier pantalla, con los resultados
agrupados en Vehículos y Clientes.

**No tiene endpoint propio.** Pregunta a los dos listados que ya existían, cada uno sólo si
quien busca puede verlo —la misma declaración del contrato de siempre—. El mecánico
encuentra autos y a los clientes ni se les pregunta; el repuestero, al revés; quien no
puede ver ninguno de los dos no tiene buscador. Un endpoint «buscar todo» habría tenido que
repetir esas reglas del lado del servidor.

Espera un cuarto de segundo después de la última tecla: una patente son siete letras
seguidas, y no hacen falta siete consultas. Se usa entero con teclado —`↓` baja a los
resultados, `Enter` abre, `Esc` cierra y deja el foco en el campo—, y el `Esc` no se
escapa al teclado global: con un formulario abierto atrás, lo cerraría también.

Los clientes todavía no tienen ficha: elegir uno abre el listado filtrado por su documento.

| | |
| --- | --- |
| Tests | **355** — 57 core · 12 afip · 99 web · 28 db · 132 api · 18 back-office api · 9 back-office web |

## La ficha del cliente

`/clientes/{id}`: los datos, los vehículos que tiene y los que tuvo con sus fechas, y la
historia contada en palabras —con los nombres del catálogo, que ahora carga una sola
función compartida con la pantalla de Auditoría—. Se modifica desde ahí con el mismo
formulario del listado.

**Los vehículos van en otro pedido**, `/clientes/{id}/vehiculos`, con permiso de ver
vehículos. El repuestero atiende clientes en el mostrador pero no ve el parque: entra a la
ficha, y los autos ni se piden. Metidos en la misma respuesta, habría hecho falta un
permiso adentro de otro. En los que ya no tiene, no se dice quién los tiene hoy: eso es de
la ficha del vehículo.

Todo quedó enlazado: el nombre en el listado de clientes, el titular en la ficha del
vehículo y el cliente en el buscador del encabezado llevan a la ficha. Si el alta choca con
un documento ya cargado, el aviso ofrece «Abrir su ficha» en vez de filtrar el listado.

### Un test que falló una vez

En una corrida de la suite completa fallaron los dos primeros tests de aislamiento, y no se
pudo reproducir: pasaron solos y en tres corridas completas más. No se inventó una causa.
Lo que se cambió es que su `entrar` ahora revisa que el login responda 200 y, si no, dice
qué contestó: antes, un login fallido se veía como un «total» equivocado, que es justo el
síntoma de un cruce de datos, y confunde. Si vuelve a pasar, va a decir por qué.

| | |
| --- | --- |
| Tests | **363** — 57 core · 12 afip · 103 web · 28 db · 136 api · 18 back-office api · 9 back-office web |

## Roles

Lo último del Hito 2 con pantalla. El detalle quedó en
[usuarios y roles](../tecnicos/usuarios-y-roles.md); lo que se decidió:

- **Casillas, y lo que no cabe se conserva.** El mecánico tiene «edita sólo sus órdenes», una
  condición que ninguna casilla expresa. Una grilla ingenua la habría borrado al primer
  guardar. Ahora se muestra en palabras y la API la mantiene; clonar la copia.
- **Nadie edita el rol que tiene.** Apareció al pensarlo: el administrador de sistema no puede
  cambiarse sus roles, pero podía cambiar lo que su rol da.
- **El que puede todo no se edita**, ni el gerente: se clona. Un «Subgerente» es clonar
  Gerente —arranca con todo marcado— y destildar lo que no va.
- **Vale en el acto y se avisa.** La API lee los permisos en cada pedido; al mecánico le llega
  «cambió el rol Mecánico: ahora podés ver clientes». El test lo prueba con el token de
  antes del cambio.

De paso, `core` expone los nombres de acciones y sujetos, que hasta ahora sólo se usaban
adentro de las frases de permiso.

| | |
| --- | --- |
| Tests | **385** — 64 core · 12 afip · 109 web · 28 db · 145 api · 18 back-office api · 9 back-office web |

## Dónde quedó

Todo commiteado y **sin pushear**. 385 tests en verde, typecheck y lint limpios.

**El Hito 2 queda con todas sus pantallas.** Lo que falta de él: registrar las pantallas en
el buscador del sistema —que todavía no existe— y la historia de órdenes en la ficha del
vehículo, que llega con el Hito 4.

Para retomar:

1. Decidir entre el buscador del sistema (`F10`, cierra del todo el Hito 2) y arrancar el
   Hito 3, facturación electrónica.
2. Pendientes anotados: proveedores con el módulo de repuestos, el segundo factor de los
   operadores, y vigilar el test de aislamiento que falló una vez.

Entorno local: Docker con `gpb-pg` y `gpb-redis`; `.env` con los usuarios `gpb_*` y las
variables `AFIP_PADRON_*` apuntando a `secretos/afip/`. Las apps se levantan con
`pnpm dev` y `pnpm dev:backoffice`; Swagger queda en <http://localhost:3080/api/docs>.
Docker Desktop no arranca solo con Windows: sin él, los tests de la API fallan con «Could
not find a working container runtime strategy».
